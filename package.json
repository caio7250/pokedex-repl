{
  "name": "pokedex",
  "version": "1.0.0",
  "description": "CLI Pokedex that consumes the PokeAPI (Node + TypeScript)",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "tsc && node dist/main.js",
    "test": "vitest --run"
  },
  "keywords": ["pokedex", "pokeapi", "cli"],
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
